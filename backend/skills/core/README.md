This folder contains LLM-first skills designed for the tool-calling planner system. Each skill MUST export a TOOL_SCHEMA dict in addition to the usual SKILL_ID, DESCRIPTION, and async execute function. These skills are discovered automatically by backend/engine/planner/skill_registry.py at startup. Legacy skills in the parent backend/skills/ folder are not discovered by the planner and remain used by the existing linear pipeline system.
